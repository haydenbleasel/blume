---
title: Deploy
description: Deploy your Blume docs to any static host.
sidebar:
  order: 1
---

# Deploy

Build your docs and deploy the `dist/` folder to any static host.

```bash
blume build
```

## Vercel

Blume works cleanly on Vercel with static output by default, and supports
server output through `@astrojs/vercel` when you need dynamic features.
