---
title: Setup
description: Install and configure the project.
full: true
---

import { Accordion, Accordions } from "fumadocs-ui/components/accordion";
import { File, Folder, Files } from "fumadocs-ui/components/files";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";

# Setup

Pick your package manager:

<Tabs items={["npm", "pnpm", "yarn"]}>
  <Tab value="npm">Install with npm.</Tab>
  <Tab value="pnpm">Install with pnpm.</Tab>
  <Tab value="yarn">Install with yarn.</Tab>
</Tabs>

<Accordions>
  <Accordion title="Why a build step?">
    Blume generates a hidden Astro project.
  </Accordion>
</Accordions>

<Files>
  <Folder name="src">
    <File name="index.ts" />
  </Folder>
</Files>
