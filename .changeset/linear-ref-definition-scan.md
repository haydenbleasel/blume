---
"blume": patch
---

Scan link-reference definitions in linear time. A line of deeply nested block-quote or list containers that never reaches a `[label]:` no longer backtracks exponentially while headings are extracted.
