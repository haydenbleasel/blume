---
"blume": patch
---

Restyle collapsible sidebar group headers to sit back visually: the group `<summary>` now renders in the muted foreground color and brightens to the full foreground on hover, matching the treatment of leaf nav links. The active-page group header still resolves to the foreground color and bold weight so the current section stays legible.
