---
"blume": patch
---

An operation description whose first paragraph ends in a colon, as a lead-in to a list does ("Supports two modes:"), no longer ends its meta description in `:.`. The colon becomes the period, so the page reads "Supports two modes. Reference for the … endpoint". The same applies to operation summaries on the agent surfaces.
