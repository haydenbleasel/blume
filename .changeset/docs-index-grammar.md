---
"blume": patch
---

The docs landing page's customization sentence read awkwardly ("start replacing … file or even ejecting"); it now reads "start by replacing the built-in components, modifying the single configuration file, or even ejecting".
