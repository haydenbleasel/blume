---
"blume": patch
---

The "On this page" highlight now skips headings inside tab panels that aren't selected. Such a heading used to count as already scrolled past, so the table of contents highlighted it from the top of the page.
