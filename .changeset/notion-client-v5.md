---
"blume": minor
---

The Notion source now targets `@notionhq/client` v5 and Notion's data source API: a database is read through its first data source. Upgrade the peer dependency with `npm install @notionhq/client@^5`.
