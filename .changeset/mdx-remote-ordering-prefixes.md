---
"blume": patch
---

`mdxRemote()` pages lose their file and folder names' ordering prefixes the way a local content folder's do: `01-getting-started/02-install.mdx` now routes to `/sdk/getting-started/install` instead of `/sdk/01-getting-started/02-install`, while the prefixes still order the sidebar.
