---
"blume": patch
---

A header link, featured link, or other configured link that already carries `deployment.base` followed by a fragment or query, like `/sub#install` under `base: "/sub"`, is no longer prefixed a second time. It used to render as `/sub/sub#install`.
