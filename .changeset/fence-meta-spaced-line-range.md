---
"blume": patch
---

Keep a spaced Shiki line range (` ```ts {1, 3-5} `) as one fence-meta token. The range used to split on its whitespace, and the `3-5}` fragment was promoted to the code block's title.
