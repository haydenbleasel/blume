---
"blume": patch
---

The update-docs skill claimed `blume build` validates links and anchors; it now runs `blume validate` for link and anchor checks, with build covering frontmatter and duplicate routes.
