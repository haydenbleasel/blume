---
"blume": patch
---

The deployment docs claimed every adapter is pulled in automatically; they now say `vercel` and `node` ship with Blume while `@astrojs/netlify` and `@astrojs/cloudflare` must be installed in the project.
