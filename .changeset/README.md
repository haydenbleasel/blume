# Changesets

This directory is managed by [Changesets](https://github.com/changesets/changesets).

Run `bunx changeset` to describe a change. Each changeset is a small Markdown file recording the affected package, the semver bump, and a summary. Merging changesets to `main` opens a release PR.
