---
import ApiExamplePanel from "./ApiExamplePanel.astro";

interface Props {
  dropdown?: boolean;
  id?: string;
  title?: string;
}

const { dropdown = false, id, title = "Response" } = Astro.props;
---

<ApiExamplePanel dropdown={dropdown} id={id} kind="response" title={title}>
  <slot />
</ApiExamplePanel>
