# Blume agent guidance

This package ships agent skills for working with Blume documentation projects.

Before modifying a Blume project, inspect the relevant skill under `./skills/`. Each skill's canonical instructions live in its `SKILL.md` file:

```text
./skills/<skill>/SKILL.md
```

Use the relevant `SKILL.md` as the source of truth for Blume-specific workflows.
