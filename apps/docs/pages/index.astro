---
import PageLayout from "blume/components/layout/PageLayout.astro";
import data from "blume:data";
import AgentNative from "./_home/AgentNative.astro";
import Batteries from "./_home/Batteries.astro";
import Cli from "./_home/Cli.astro";
import Components from "./_home/Components.astro";
import ContentSources from "./_home/ContentSources.astro";
import CustomerStories from "./_home/CustomerStories.astro";
import Footer from "./_home/Footer.astro";
import ForAgents from "./_home/ForAgents.astro";
import Hero from "./_home/Hero.astro";
import InstallCta from "./_home/InstallCta.astro";
import LogoGrid from "./_home/LogoGrid.astro";
import MarkdownShowcase from "./_home/MarkdownShowcase.astro";
import Migrate from "./_home/Migrate.astro";
import Motion from "./_home/Motion.astro";
import { installCopyScript } from "./_home/shared.ts";
import Sponsors from "./_home/Sponsors.astro";

// Everything the landing page needs comes from the resolved Blume config, so it
// stays in sync with the docs site (title, description, banner, theme mode).
// PageLayout supplies the document shell, header, theme, and fonts — the
// document `<html>`/`<head>`, theme + banner pre-paint scripts, and banner that
// this page used to hand-roll all live there now.
const { config } = data;
const pageTitle = `${config.title} — the open-source docs framework for humans and agents`;
// A landing-page-specific meta description so the home page reads distinctly
// from the site-wide default (which other pages fall back to).
const pageDescription =
  "The open-source docs framework for humans and agents. Drop Markdown into a folder and ship a fast docs site with search, API references, and llms.txt.";
---

<PageLayout
  analytics={config.analytics}
  banner={config.banner}
  favicon={config.favicon}
  fontCssVars={data.fontCssVars}
  logo={config.logo}
  navigation={data.navigation}
  ogEnabled={config.og.enabled}
  ogImage="/opengraph-image.png"
  ogImageAlt={pageTitle}
  ogImageSize={{ height: 630, width: 1200 }}
  page={{ description: pageDescription, route: "/", title: pageTitle }}
  searchEnabled={config.search.enabled}
  site={{ description: config.description, title: config.title }}
  siteUrl={config.site}
  structuredDataEnabled={config.structuredData}
  themeMode={config.theme.mode}
  x={config.x}
>
  <Hero />

  <div
    class="mx-auto grid max-w-[1400px] gap-24 px-6 pb-20 sm:pb-24 lg:gap-32 xl:gap-40"
  >
    <LogoGrid />

    <CustomerStories />

    <ForAgents />

    <AgentNative />

    <Batteries />

    <MarkdownShowcase />

    <ContentSources />

    <Components />

    <Cli />

    <!-- The hero's "Migrate your docs" link lands here -->
    <div class="scroll-mt-24" id="migrate">
      <Migrate />
    </div>

    <!-- The CTA closes the story; sponsors follow it as a credit. -->
    <div class="flex flex-col gap-16">
      <InstallCta />
      <Sponsors />
    </div>
  </div>
  <!-- After the sections, so their reveal containers exist when it arms -->
  <Motion />
  <script is:inline set:html={installCopyScript} />
  <Footer slot="footer" />
</PageLayout>
