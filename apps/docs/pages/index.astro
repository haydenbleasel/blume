---
import PageLayout from "blume/components/layout/PageLayout.astro";
import data from "blume:data";
import Batteries from "./_home/Batteries.astro";
import BuiltForModels from "./_home/BuiltForModels.astro";
import ContentSources from "./_home/ContentSources.astro";
import Footer from "./_home/Footer.astro";
import Hero from "./_home/Hero.astro";
import InstallCta from "./_home/InstallCta.astro";
import MarkdownShowcase from "./_home/MarkdownShowcase.astro";
import Migrate from "./_home/Migrate.astro";
import ProductPreview from "./_home/ProductPreview.astro";
import Spotlight from "./_home/Spotlight.astro";

// Everything the landing page needs comes from the resolved Blume config, so it
// stays in sync with the docs site (title, description, banner, theme mode).
// PageLayout supplies the document shell, header, theme, and fonts — the
// document `<html>`/`<head>`, theme + banner pre-paint scripts, and banner that
// this page used to hand-roll all live there now.
const { config } = data;
const pageTitle = `${config.title} — fast, AI-ready, markdown-first docs`;

// Copy-to-clipboard for the install boxes; briefly swaps the glyph for a check.
// One delegated handler covers every box on the page. Page-specific (the docs
// chrome's copy buttons are separate), so it stays here.
const copyScript = `(()=>{const CHECK='<svg class="size-4" fill="none" height="16" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" viewBox="0 0 24 24" width="16"><path d="M20 6 9 17l-5-5"/></svg>';document.addEventListener("click",async(e)=>{const b=e.target.closest("[data-blume-copy-install]");if(!b){return;}try{await navigator.clipboard.writeText(b.dataset.command);}catch{return;}const o=b.innerHTML;b.innerHTML=CHECK;setTimeout(()=>{b.innerHTML=o;},1500);});})();`;
---

<PageLayout
  analytics={config.analytics}
  banner={config.banner}
  favicon={config.favicon}
  fontCssVars={data.fontCssVars}
  logo={config.logo}
  navigation={data.navigation}
  ogEnabled={config.og.enabled}
  page={{ description: config.description, route: "/", title: pageTitle }}
  searchEnabled={config.search.enabled}
  site={{ description: config.description, title: config.title }}
  siteUrl={config.site}
  themeMode={config.theme.mode}
>
  <div
    class="mx-auto grid max-w-[1400px] gap-16 px-6 py-20 sm:py-24 lg:gap-24 xl:gap-32"
  >
    <Hero />

    <ProductPreview />

    <Batteries />

    <MarkdownShowcase />

    <ContentSources />

    <BuiltForModels />

    <Spotlight />

    <Migrate />

    <InstallCta />
  </div>
  <script is:inline set:html={copyScript} />
  <Footer slot="footer" />
</PageLayout>
