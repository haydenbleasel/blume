---
// The customers index (/customers): every story's panel (from
// _customers/customers.ts), the wall of teams building on Blume, and the
// install CTA. Built in the homepage's design language, like the stories it
// links to.
import PageLayout from "blume/components/layout/PageLayout.astro";
import data from "blume:data";
import Directory from "../_customers/Directory.astro";
import LogoWall from "../_customers/LogoWall.astro";
import Footer from "../_home/Footer.astro";
import InstallCta from "../_home/InstallCta.astro";
import Motion from "../_home/Motion.astro";
import { installCopyScript } from "../_home/shared.ts";

const { config } = data;
const pageTitle = "Blume customer stories";
const pageDescription =
  "How product teams and open-source projects moved their docs to Blume, and what changed for their readers and their agents.";
---

<PageLayout
  analytics={config.analytics}
  banner={config.banner}
  favicon={config.favicon}
  fontCssVars={data.fontCssVars}
  logo={config.logo}
  navigation={data.navigation}
  ogEnabled={config.og.enabled}
  page={{ description: pageDescription, route: "/customers", title: pageTitle }}
  searchEnabled={config.search.enabled}
  site={{ description: config.description, title: config.title }}
  siteUrl={config.site}
  structuredDataEnabled={config.structuredData}
  themeMode={config.theme.mode}
  x={config.x}
>
  <div
    class="mx-auto grid max-w-[1400px] gap-24 px-6 pb-20 sm:pb-24 lg:gap-32 xl:gap-40"
  >
    <Directory />

    <LogoWall />

    <InstallCta />
  </div>
  <!-- After the sections, so their reveal containers exist when it arms -->
  <Motion />
  <script is:inline set:html={installCopyScript} />
  <Footer slot="footer" />
</PageLayout>
